<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no" name="viewport">
  <title><%= content_for?(:title) ? yield(:title) : "Business Management" %></title>
  <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Online Business Management Solution" %>">
  <%= stylesheet_link_tag 'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>
  <!-- HTML5 Shim and Respond.js IE8 support of HTML5 elements and media queries -->
  <!--[if lt IE 9]>
  <script src="https://oss.maxcdn.com/html5shiv/3.7.3/html5shiv.min.js"></script>
  <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
  <![endif]-->
  <%= csrf_meta_tags %>

</head>
<body class="hold-transition skin-blue layout-top-nav">
<div class="wrapper">

  <!-- Header -->
  <%= render partial: "_partials/header" %>

  <!-- Full Width Column -->
  <div class="content-wrapper">
    <div class="container">
      <!-- Content Header (Page header) -->
      <section class="content-header">
        <% if content_for?(:page_header_block) %>
            <%= yield(:page_header_block) %>
        <% else %>
            <h1>
              <%= content_for?(:page_header) ? yield(:page_header) : "Business Management" %>
              <small><%= content_for?(:page_subheader) ? yield(:page_header) : "" %></small>
            </h1>
        <% end %>
      </section>

      <!-- Main content -->
      <section id="main-content" class="content">
        <div class="box box-default">
          <div class="box-body">
            <%= flash_messages %>
            <%= yield %>
          </div><!-- /.box-body -->
        </div><!-- /.box -->
      </section><!-- /.content -->
    </div><!-- /.container -->
  </div><!-- /.content-wrapper -->

  <!-- Footer -->
  <%= render partial: "_partials/footer" %>

</div>
<!-- ./wrapper -->

</body>
</html>